services:
  # Web server service
  apache:
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "8080:80"
    container_name: kubbi_apache
    restart: unless-stopped
    volumes:
      - ./:/var/www/html/
